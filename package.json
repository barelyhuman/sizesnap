{
  "name": "sizesnap-packages",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=12",
    "pnpm": ">=3"
  },
  "private": "true",
  "scripts": {
    "server:dev": "pnpm run dev --filter server",
    "build": "pnpm run build -r"
  },
  "dependencies": {
    "brotli-size": "^4.0.0",
    "bytes": "^3.1.1",
    "got": "^12.0.1",
    "picocolors": "^1.0.0",
    "tar": "^6.1.11",
    "tiny-glob": "^0.2.9",
    "zlib": "^1.0.5"
  },
  "devDependencies": {
    "@changesets/cli": "^2.20.0"
  }
}
